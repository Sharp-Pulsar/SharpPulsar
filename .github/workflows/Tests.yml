# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Tests --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Tests

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - name: Setup SDKs
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: |
            5.0.*
            6.0.*

      - uses: actions/checkout@v1
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
     
      - name: Run 'StartPulsar'
        run: ./build.cmd StartPulsar
     
      - name: Run 'AdminPulsar'
        run: ./build.cmd AdminPulsar
     
      - name: Run 'Tests'
        run: ./build.cmd Test
     
      - name: Run 'Txn Tests'
        run: ./build.cmd TxnTest
     
      - name: Run 'StopPulsar'
        run: ./build.cmd StopPulsar
